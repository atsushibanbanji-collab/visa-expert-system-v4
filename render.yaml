services:
  - type: web
    name: visa-expert-backend
    env: python
    region: oregon
    runtime: python-3.11
    buildCommand: "cd backend && pip install -r requirements.txt"
    startCommand: "cd backend && python migrate_rules.py && python migrate_add_derivable_questions.py && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: visa-expert-db
          property: connectionString
      - key: ADMIN_USERNAME
        value: admin
      - key: ADMIN_PASSWORD
        generateValue: true

  - type: web
    name: visa-expert-frontend
    env: static
    buildCommand: "cd frontend && npm install && npm run build"
    staticPublishPath: frontend/dist
    envVars:
      - key: VITE_API_URL
        value: https://visa-expert-backend-h2oa.onrender.com/api
    routes:
      - type: rewrite
        source: /api/*
        destination: https://visa-expert-backend-h2oa.onrender.com/api/*
      - type: rewrite
        source: /*
        destination: /index.html

databases:
  - name: visa-expert-db
    region: oregon
    plan: free
